# Quick reference (optional prefix)

Same domain rules as the bundled skill `coindcx_futures_trading` under `config/ollama_agent/skills/`. Prefer running `bin/coindcx-agent` so that skill directory is loaded into the agent system prompt automatically.
